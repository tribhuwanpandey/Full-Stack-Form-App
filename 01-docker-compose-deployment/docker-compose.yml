services:

  frontend:
    build: ../base-image/frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build: ../base-image/backend
    ports:
      - "5000:5000"